{
  "contract_id": "c_0005",
  "repo": "/Users/kurultai/llm-survivor",
  "base_ref": "main",
  "objective": "Create backend/dedupe.py with unique_preserving_order(items) that removes duplicates while preserving first-occurrence order, using a set for membership testing so it stays linear. Add backend/tests/test_dedupe.py asserting order is preserved. Do not modify any existing file.",
  "context_refs": [
    "backend/**"
  ],
  "v1_command": "uv run --quiet --with pytest python -m pytest backend/tests -q",
  "seeded_class": "perf_regression",
  "notes": "Injection turns the set into a list: behaviourally identical, so V1 stays green and only a reviewer reading the diff can see the complexity regression."
}
