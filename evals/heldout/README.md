# heldout/ — WRITE-PROTECTED

Operator-rotated monthly: ~20% of benchmark contracts move here and the manifest hash is
logged. The loop never trains on anything whose hash appears here.

Empty until the first rotation (post-B6). Do not add contracts by hand mid-cycle — the
rotation date and the hash log are what make this a control rather than a folder.
