{
  "contract_id": "c_0002",
  "repo": "/Users/kurultai/llm-survivor",
  "base_ref": "main",
  "objective": "Create backend/pagination.py with paginate(items, page, page_size) returning the correct slice for 1-indexed pages, and total_pages(items, page_size). Add backend/tests/test_pagination.py covering the first page, a middle page, and a final partial page. Do not modify any existing file.",
  "context_refs": [
    "backend/**"
  ],
  "v1_command": "uv run --quiet --with pytest python -m pytest backend/tests -q",
  "seeded_class": "off_by_one",
  "notes": "Injection perturbs the slice arithmetic. The new tests should catch it -- this is the control showing V1 CAN see some classes."
}
