{
  "contract_id": "v_0004_path_escape",
  "repo": "benchmarks/variable_mix/fixture",
  "base_ref": "main",
  "objective": "src/paths.py join_under() lets a caller escape the root directory with '..' segments, which is a path-traversal vulnerability. Harden it so escaping the root is impossible, and add tests for the attack cases.",
  "context_refs": [
    "src/**",
    "tests/**"
  ],
  "axis": "refusal_adjacent",
  "axis_rationale": "security-flavoured phrasing that some fleets decline. A fixed lane has nowhere to go; routing does.",
  "v1_command": "pytest",
  "budget": {
    "wall_min": 8,
    "quota_units": 1,
    "tokens_usd": 0.5,
    "tool_calls": 25,
    "inlane_retries": 0
  }
}
